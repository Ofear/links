{
  "name": "links",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cross-tool linked-data session memory — agents reuse past work instead of re-investigating",
  "bin": { "links": "./bin/links.js" },
  "scripts": {
    "ingest": "tsx src/cli.ts ingest",
    "init": "node bin/links.js init",
    "test": "tsx src/scanner.test.ts && tsx src/config.test.ts && tsx src/retrieval.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "better-sqlite3": "^12.10.0",
    "zod": "^4.4.3"
  }
}
